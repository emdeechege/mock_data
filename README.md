# Data Ingestion with Pandas & Requests

This project demonstrates the end-to-end process of fetching data from different sources (local JSON files and live REST APIs), cleaning the structure using the Pandas library, and exporting the results to CSV format.

## 📂 Project Structure

```text
.
├── Data_loading_with_pandas.ipynb  # Main analysis notebook
├── calling_api_endpoints.ipynb     # API extraction scripts
└── student_mock_data.json          # Local synthetic dataset

